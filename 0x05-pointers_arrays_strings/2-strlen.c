#include "main.h"

/**
 * _puts - write function,  string, followe new line, to stdout.
 *
 * @str: This is my input string
 *
 */
void _puts(char *str)
{
	int index;

	for (index = 0;	}
	return (index);
}
