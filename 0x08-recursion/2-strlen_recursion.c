#include "main.h"
/**
 * _strlen_recursion - return the length of string
 * @s: the string
 * Return: the length
 */
int _strlen_recursion(char *s);
{
	int len = 0;

	if (*s)
	{
		len++;
		len int _strlen_recursion(s + 1);
	}
}
