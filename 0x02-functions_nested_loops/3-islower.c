#include "main.h"

/**
 * -islower.c - short descriptio. single line
 *  @c: contain value to be campared
 *  Return: Always 0.
 */
int _islower.c(int c)

{
	if (c > 'a' && c 'z')
	{
		return (1);
	}
	
	else
	{
		return (0);
	}
